name: Test Notification Secret

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test NOTIFICATION_SERVICE_URL
        run: |
          if [ -z "${{ secrets.NOTIFICATION_SERVICE_URL }}" ]; then
            echo "::error::NOTIFICATION_SERVICE_URL is not configured"
            exit 1
          else
            echo "✅ NOTIFICATION_SERVICE_URL is configured: ${{ secrets.NOTIFICATION_SERVICE_URL }}"
          fi
